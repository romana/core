services:
  - service: root
    api:
      host: localhost
      port: 8000
  - service: ipam
    api:
      host: localhost
      port: 8001
    config:
      store: 
        type: mock
        host: localhost
        username: root
        password: password
        database: ipam
  - service: tenant
    api:
      host: localhost
      port: 8002
    config:
      store: 
        type: mock
        host: localhost
        username: root
        password: password
        database: tenant
  - service: topology
    api:
      host: localhost
      port: 8003
    config:
      store: 
        type: mock
        host: localhost
        username: root
        password: password
        database: topology